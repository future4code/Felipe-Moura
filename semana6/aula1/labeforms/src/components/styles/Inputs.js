import styled from 'styled-components'

const Inputs = styled.input`
    width: 50%;
    padding: 1vh;
`
const Selections = styled.select`
    width: 50%;
    padding: 1vh;
`

export {Inputs, Selections}